import PropTypes from 'prop-types';

export default PropTypes.shape({
  BUTTON_CLASS_NAME: PropTypes.string,
  BUTTON_CLASS_NAME_ACTIVE: PropTypes.string,
  SVG_CLASS_NAME: PropTypes.string,
  SVG_WIDTH: PropTypes.string,
  SVG_HEIGHT: PropTypes.string,
});
